# NOVA – PURCHASE & STORE HUB (P&S Hub) — ความจำถาวรของโปรเจกต์

> อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้ง · Candy = ชื่อที่เบียร์ (sasipa@suteetankers.com) เรียก Claude
> **เวอร์ชันจริงอยู่บรรทัดแรกของ `deploy/CHANGELOG.md` เสมอ** — ห้ามเชื่อเลขที่อื่น

---

## 0. โปรเจกต์นี้คืออะไร

เว็บแอป **แยกจาก NOVA ใหญ่** ใช้ภายในสำหรับ **จัดซื้อ + สโตร์** โดยเฉพาะ

| | NOVA ใหญ่ | P&S Hub (ตัวนี้) |
|---|---|---|
| ทำอะไร | เก็บต้นทุนตามจ๊อบที่เปิด (BOM) | ระบบจัดซื้อจัดจ้าง + คลังสินค้า |
| Script ID | `1hskE-6uACo3CwnR41-Hc4U2v9L1iOIxbCOjT8UMaqVszYt_Y2NqRo6su` | `11omqFMc3yIYDEernlRYhafVNDHWSA2NCFmuEWiIl_weebnn0OHVbsUT9` |
| GitHub | `STT-WEB/STT-NOVA` | `STT-WEB/STT-NOVA-PS-HUB` |

**ทิศทางข้อมูล:** My Account → **P&S Hub** → (ราคาซื้อล่าสุด + ต้นทุนเฉลี่ยกลาง) → NOVA ใหญ่
→ **P&S Hub เป็นเจ้าของราคา · NOVA เป็นผู้ใช้**

---

## ⚠️ 10 ข้อที่ต้องทำ (เหมือน NOVA — ใช้ร่วมกัน)

1. **เบียร์เอาหลักฐาน ไม่เอาคำรับรอง** — ต้องมีตัวเลขวัดจริง หรือผลทดสอบมายืนยัน
2. **ห้ามเดา** ไม่รู้ให้บอกว่าไม่รู้ แล้วไปวัด/ไปอ่านโค้ดก่อน
3. **ทำผิดต้องบอกทันที ห้ามกลบ** — เขียนลง CHANGELOG ตรง ๆ
4. **อธิบายเป็นภาษาคน** เบียร์ไม่ใช่โปรแกรมเมอร์ แต่เข้าใจ logic ดีมาก
5. **ทำงานเป็นขั้น 1-2-3 เสมอ** เพราะเบียร์ต้องเอาไปพรีเซ้นและสอนพนักงาน
6. **เจอปัญหา 1 จุด ต้องไล่ให้ครบทุกจุดที่เป็นแบบเดียวกัน**
7. **ต้องใช้ได้อีก 10-20 ปี** — ของใหม่ให้แยกไฟล์ อย่าไปแตะทางเดินที่ใช้งานอยู่
8. **ทุกครั้งที่แก้โค้ด ต้องมีชุดทดสอบกันพัง** ใน `_tests/`
9. **งานใหญ่ถามก่อนทำ** เสนอทางเลือกพร้อมข้อดีข้อเสีย แล้วให้เบียร์เคาะ
10. **เบียร์เรียก Claude ว่า "Candy"**

## 🚫 6 ข้อที่ห้ามทำเด็ดขาด

1. **ห้ามแก้ไฟล์ที่ไม่ใช่บนเครื่องเบียร์** — แก้สำเนาในคลาวด์ = งานหายทั้งหมด
2. **ห้ามบอกว่า deploy สำเร็จ ถ้ายังไม่เห็นป้ายเวอร์ชันเปลี่ยน** — ข้อความ DONE ใน .bat เชื่อไม่ได้
3. **ห้ามให้สโตร์เห็นราคา/ต้นทุน** — ต้องตัดที่ฝั่งเซิร์ฟเวอร์ด้วย `requireNotStore_()` ไม่ใช่ซ่อนคอลัมน์
   → `PRICE_ROLES` ห้ามมี `STORE` เด็ดขาด (ชุดทดสอบเช็คข้อนี้)
4. **ห้ามฝัง File ID ของไฟล์รายปีในโค้ด** — ต้องอ่านจาก `REGISTRY` (source = `PS`) เท่านั้น
5. **ห้ามลบแถวใน `RR_ALL`** — Carry Forward ใช้ผลรวมสะสม ลบแล้วสูตรพัง (ใช้เขียนทับ `PRICE_LATEST` แทน)
6. **ห้ามใส่โทเคน/รหัสลับในโค้ด** — ใช้ Script Properties

## ข้อห้ามเฉพาะจุด

- `getEmail_()` ใช้ `getActiveUser` เท่านั้น — **ห้าม fallback `getEffectiveUser`** (ทุกคนจะกลายเป็น ADMIN)
- `roleOf_()` ต้องเชื่อ **PIN ก่อน** แล้วค่อย fallback อีเมล (บทเรียน NOVA v0.38.0)
- `.bat` ต้องเป็น **ASCII ล้วน** (ภาษาไทยในเนื้อไฟล์ทำ cmd พัง · ชื่อไฟล์ไทยได้)
- ห้ามมี `?>` ในโค้ด JS ของไฟล์ `.html` (ชน template ของ Apps Script)
- ห้ามถอด `Sheets` ออกจากบริการขั้นสูงใน Apps Script (ใช้ `fetchTabValues_`)
- ไฟล์โค้ด 1 ไฟล์ **ไม่เกิน ~1,500 บรรทัด** (บทเรียน NOVA: ไฟล์เดียว 807 KB)

---

## 1. โครงไฟล์ข้อมูล (Google Drive)

โฟลเดอร์หลัก: `NOVA-Purchase&Store Hub` — `1rVHUm8GdXsG_VElRFuIoAX-E2o24bQmE`

| ไฟล์ | โตแบบไหน | ปีที่ 20 | % เพดาน 10 ล้านเซลล์ |
|---|---|---|---|
| `STT-PS-<ปี>` (1 ปี 1 ไฟล์) | ไม่โต — ปีใหม่เริ่มไฟล์ใหม่ | 1,559,134 | 15.6% |
| `STT-PS-RR-ALL` (ไฟล์เดียวตลอด) | สะสมทุกปี 2562→ | 3,462,680 | 34.6% |
| `STT-DB-MASTER` (ใช้ร่วมกับ NOVA) | สะสม (DOC_INDEX) | 6,212,900 | 62.1% |

**ห้ามยัด RR-ALL รวมกับ MASTER** — จะได้ 96.8% ของเพดาน ไม่ปลอดภัย

## 2. สิทธิ์ผู้ใช้

ทะเบียน = แท็บ `USERS` ใน `STT-DB-MASTER` (ใช้ร่วมกับ NOVA)
`A email · B display_name · C role for NOVA BOM · D role for Claim · E role for P&S · F active · G PIN · H รหัสพนักงาน`

- ไฟล์นี้อ่าน **คอลัมน์ E เท่านั้น** และค้นด้วยชื่อหัวตาราง (`colIdx_`) ไม่ใช่ตำแหน่ง
- บทบาท: `PURCHASE` · `STORE` · `ADMIN` · `EXEC`
- เห็นราคาได้: `PURCHASE` `ADMIN` `EXEC` — **สโตร์ไม่เห็น**
- เข้าระบบด้วย **รหัสพนักงาน + PIN 6 หลัก** (พนักงาน 42 คน มีอีเมลแค่ 9 คน)

## 3. โมดูลทั้งหมด 22 ตัว

**จัดซื้อ (P)** P1 นำเข้า+จับคู่ · P2 ค้างรับ · P3 แผนจ่ายเงิน · P4 สรุปสั่งซื้อ · P5 ผู้รับเหมา ·
P6 ลิงก์ PR · P7 Dashboard KPI · P8 Performance พนักงาน · P9 วางแผนซื้อล่วงหน้า · P10 นำเข้า+มัดจำ
**ต้นทุน (C)** C1 Costing Engine · C2 Sales ขอราคา
**สโตร์ (S)** S1 รับของ GRN · S2 Stock Card 3 คลัง · S3 นับสต๊อกเดือน · S4 มูลค่า+ABC · S5 แนะนำ Stock
**งานระบบ (X)** X1 ยามตรวจข้อมูล · X2 แจ้งเตือน · X3 ตั้งค่า/สิทธิ์ · X4 ประวัติการใช้งาน · X5 สำรอง+ปิดปี

## 4. วิธี DEPLOY

**workflow: Candy แก้ไฟล์ใน `deploy/` → เบียร์ดับเบิลคลิก `อัปเดตระบบ.bat`**
(git commit + push GitHub → clasp push ทุกไฟล์ → clasp deploy = เผยแพร่เวอร์ชันใหม่)

- ครั้งแรกสุด: `ติดตั้งเครื่องมือครั้งแรก.bat` → `เข้าสู่ระบบ clasp.bat` → `อัปเดตระบบ.bat`
- `clasp push -f` อัปทุกไฟล์ในโฟลเดอร์ `deploy/` **ไม่ต้องทำทีละไฟล์**
- deployment id เก็บอัตโนมัติที่ `deploy/.deployid` (ไม่ขึ้น git · ไม่ขึ้น Apps Script)
- **หลักฐานเดียวที่พิสูจน์ว่าขึ้นจริง = ป้ายเวอร์ชันมุมขวาบนเปลี่ยนเป็นเลขใหม่**
  ป้ายดึงจาก `PS_VERSION` ใน `deploy/00-config.js` โดยตรง → มีที่เดียว แก้ที่เดียว

## 5. ไฟล์โค้ด

| ไฟล์ | ทำอะไร |
|---|---|
| `deploy/00-config.js` | เวอร์ชัน · CFG · TTL · PRICE_ROLES · doGet · include |
| `deploy/01-auth.js` | ล็อกอิน empId+PIN · บทบาท · `requireNotStore_` |
| `deploy/02-master.js` | `fetchTabValues_` · แคช · `colIdx_` · REGISTRY · `yearFile_` |
| `deploy/99-diag.js` | ตรวจระบบด้วยการวัดจริง (`psSelfTest` / `psDiag`) |
| `deploy/PS-Hub-Index.html` | หน้าเว็บหลัก + ป้ายเวอร์ชัน |
| `deploy/styles.html` | CSS |
| `deploy/js-core.html` | โค้ดฝั่งหน้าเว็บ (ล็อกอิน · เมนู · ตรวจระบบ) |
| `_tests/` | ชุดทดสอบกันพัง — รัน `node _tests/guard-v0100.js` |
